package util.encode;

import java.util.Arrays;
import java.util.stream.Stream;

public class Hasher {
/*    private static final String DEFAULT_SALT = "DEF_SALT";
    private static final int DEFAULT_DIGREE = 3;

    public static String hash(String message, String salt, int digree) {
        byte[] byteArr = Encoder.decode(message+salt, Base64Encorder.getInstance());
        byte byteVal = 0;
        for(byte b : byteArr) {
            byteVal &= b;
        }
        byteArr = new byte[]{byteVal};
        return Encoder.encode(byteArr);
    }

    public static String hash(String message, String salt) {
        return hash(message, salt, DEFAULT_DIGREE);
    }

    public static String hash(String message) {
        return hash(message, DEFAULT_SALT);
    }*/
}
